# GeoGraphy 実装計画書 v4.0

> 要件定義書 v2.0 に基づく実装計画・開発フロー・Day 実績記録

| 項目 | 内容 |
|---|---|
| アプリ名 | GeoGraphy |
| 実装計画書バージョン | 4.0.0 |
| 対応要件定義書 | v2.0 |
| 前バージョン | アーカイブ: docs/archive/2026-04-12_Day58_実装計画書_v4.0.md |
| 更新日 | 2026-04-12（Day58 Transport Architecture 完了反映） |
| 実行環境 | Mac / Node.js v25 / Vite / TypeScript / pnpm v10.32+ / Electron 41 |
| レンダラー | Three.js r160+ |
| 開発スタイル | SDD × CDD（仕様駆動 × コンパイラ駆動） |

---

## 1. 開発の基本方針

### 1.1 SDD × CDD 原則

- **SDD（Spec-Driven Development）**: 実装前に必ず `docs/spec/` の spec ファイルを読む。仕様変更はコードより先に spec を修正する
- **CDD（Compiler-Driven Development）**: 完了条件は `pnpm tsc --noEmit`（型エラーゼロ）+ `pnpm test --run`（全テストグリーン）の両方通過
- **`any` 禁止**: 型エラーは自律修正する
- **プラン先行**: 実装前にプランを提示・承認を得てから実装開始

### 1.2 開発サイクル

```
1. docs/spec/[機能].spec.md を確認（または新規作成）
2. プランを提示・承認を得てから実装開始
3. 実装 → pnpm tsc --noEmit → 型エラーがあれば自律修正
4. pnpm test --run → 全グリーンになるまでループ
5. docs/progress/[task].log.md に完了を記録
6. セッション終了時に git commit + Day タグ（git tag dayN && git push origin dayN）
7. HANDOVER.md を更新
8. Obsidian dev-log に記録
```

### 1.3 ツール役割分担

| ツール | 役割 |
|---|---|
| Claude Desktop | spec 制作・要件定義・実装計画・CLAUDE.md 更新・壁打ち |
| Claude Code | spec を読んでから実装・tsc + test 通過・Git 操作 |
| Obsidian（GeoGraphy Vault） | 開発日誌・意思決定記録・YouTube 素材 |

---

## 2. フェーズ全体マップ（現在地: ★）

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | 基盤構築（Vite / React / TS / Three.js / GitHub / CI） | ✅ Day1-4 完了 |
| Phase 2 | Plugin / Driver アーキテクチャ + Interface 全定義 + Command パターン | ✅ Day5-6 完了 |
| Phase 3 | Program/Preview バス + Mixer Simple Window + Beat Cut + CrossFade | ✅ Day6-11 完了 |
| Phase 4 | レイヤーシステム + SDD 基盤（spec ディレクトリ） | ✅ Day12 完了 |
| Phase 5 | MacroKnobManager（コア）+ Macro Knob Simple Window UI | ✅ Day13-14 完了 |
| Phase 6 | FX スタック（10種）+ FX Simple Window + バグ修正 | ✅ Day15-19 完了 |
| Phase 7 | Geometry Plugin 追加 + BlendMode + Camera System（仮） | ✅ Day20-21 完了 |
| Phase 8 | Electron 化 + Preferences Panel + Plugin Lifecycle | ✅ Day22-25 完了 |
| Phase 9 | メニューバー整備 + .dmg ビルド + UI 設計見直し | ✅ Day26-27 完了 |
| Phase 10 | アーキテクチャ整理（mixers/ 分離・Simple Window 命名統一・View メニュー） | ✅ Day29-31 完了 |
| Phase 11 | Small screen 実装 | ✅ Day31 完了 |
| Phase 12 | 設計強化期（CC Standard・MacroKnob D&D仕様・MIDI設計・Shader Plugin設計） | ✅ Day32-41 完了 |
| Phase 13 | Camera Plugin 実装（独立Plugin化・isCameraUserOverridden） | ✅ Day42-46 完了 |
| Phase 14 | Preferences Panel 改修（Geometry/Camera/Preset システム） | ✅ Day47-48 完了 |
| Phase 15 | MidiManager 分離・MIDIRegistry・WindowPlugin・Transport Architecture | ✅ Day49-58 完了 |
| **Phase 16** | **Step 4: Engine ccMapService 依存解消（境界線を閉じる）** | ★ **← 現在地** |
| Phase 17 | 既存 SimpleWindow 廃止（GeometrySimpleWindow / FxSimpleWindow / CameraSimpleWindow） | ⬜ Phase 16 完了後 |
| Phase 18 | MacroKnob D&D アサイン UI 完成 | ⬜ Phase 17 完了後 |
| Phase 19 | [L1][L2][L3] タブ切り替え | ⬜ 未着手 |
| Phase 20 | Sequencer Plugin 設計・実装 | ⬜ 未着手 |
| Phase 21 | Shader Plugin 実装 | ⬜ Phase 20 完了後 |
| Phase 22 | Output Driver + 録画 UI 整備 | ⬜ 未着手 |
| Phase 23 | OSS 公開・v1 リリース | ⬜ 未定 |

---

## 3. Day 別実装実績

| Day | 日付 | 主な実装内容 | テスト数 |
|---|---|---|---|
| Day1-4 | 3/18以前 | Vite + React + TS + Three.js 基盤・GitHub・CI・grid-wave plugin | - |
| Day5 | 3/18 | Obsidian 導入・engine.ts 骨格・ParameterStore テスト | 34 |
| Day6 | 3/18 | ProgramBus・PreviewBus・SimpleMixer 骨格 | 34 |
| Day7 | 3/18 | CrossFade Transition Plugin・execute() 純粋関数化・App.tsx 統合 | 34 |
| Day8 | 3/18 | SimpleMixer ↔ ProgramBus/PreviewBus 接続・プレビューキャンバス mount | 34 |
| Day9 | 3/18 | engine.ts Plugin create() 呼び出し・初期 SceneState 生成 | 34 |
| Day10 | 3/18 | BPM クロック（clock.ts）・engine.threeClock 分離・TAP ボタン | 38 |
| Day11 | 3/18 | Beat Cut → engine.ts 接続・ラップアラウンド検出 | 38 |
| Day12-pre | 3/22 | SDD 基盤（docs/spec/ 9本）・CLAUDE.md v7・マルチエージェント設計 | 38 |
| Day12 | 3/23 | LayerManager・CSS 合成・SimpleMixer PROGRAM エリア更新 | 43 |
| Day13 | 3/23 | MacroKnobManager・normalize()・engine.handleMidiCC() | 50 |
| Day14 | 3/23 | MacroKnobPanel UI（32ノブ SVG arc・EditDialog） | 50 |
| Day15 | 3/23 | FX スタック（10種）・FxStack クラス・FX_STACK_ORDER | 61 |
| Day16 | 3/23 | engine FX 統合・createFxPlugins() ファクトリ・IFxStack 循環参照回避 | 64 |
| Day17 | 3/23 | FxControlPanel UI・engine FX API（getFxPlugins/setFxEnabled/setFxParam） | 71 |
| Day18 | 3/23 | FX ON/OFF バグ修正（update スキップ問題・レイヤー配置問題） | 71 |
| Day19 | 3/23 | FX 小数表示バグ修正・UI ショートカット（1/2/3/H/S/F/P） | 71 |
| Day20 | 3/23 | BlendMode セレクター・Contour plugin・Grid Tunnel plugin | 85 |
| Day21 | 3/23 | Camera System（CameraPreset・自動適用）・Opacity スライダー | 90 |
| Day22 | 3/23 | CLAUDE.md v8・spec 3本（electron/preferences/project-file）・壁打ち | 90 |
| Day23 | 3/23 | PreferencesPanel（Setup/Project タブ）・Plugin Lifecycle（applySetup） | 90 |
| Day24 | 3/24 | Electron Phase 1（main.js/preload.js）・geoAPI 型定義・IPC セキュリティ | 90 |
| Day25 | 3/24 | autosave（終了時保存・起動時復元）・Project File・Setup Geometry 反映 | 104 |
| Day26 | 3/24 | （詳細ログなし） | 104 |
| Day27 | 3/24 | isDev 修正・.dmg ビルド確認・メニューバー整備・.gitignore 修正 | 104 |
| Day28 | 3/24 | 壁打ち：アーキテクチャ整理・要件定義書 v1.9・実装計画書 v3.1 | 104 |
| Day29 | 3/25 | mixers/ 新設・ファイルリネーム・View メニュー・CLAUDE.md 更新 | 104 |
| Day30 | 3/25 | Phase 10 仕上げ：spec 更新・CLAUDE.md v10・バグ修正 | 104 |
| Day31 | 3/26 | Small screen 実装（drawImage・200msポーリング・preserveDrawingBuffer・DPR対応） | 104 |
| Day32 | 3/31 | 録画機能実装（captureStream・MediaRecorder・IPC save-recording） | 104 |
| Day33 | 3/31 | FX Plugin 追加（FilmPass / FreiChen / Glitch 未公開パラメーター公開） | 104 |
| Day34 | 3/31 | 壁打ち：Shader Plugin 独立型確定・GeoGraffi 別アプリ確定・Shader Plugin spec 初稿 | 104 |
| Day35 | 3/31 | 壁打ち：MacroKnob = コア固定・MIDI 2.0 IPC 設計・Sequencer→MacroKnob 経由設計確定 | 104 |
| Day36 | 4/2 | CC Standard v0.1 策定（Block 1xx〜9xx）・FX パラメーター対照表・映像設計提案書 | 104 |
| Day37 | 4/2 | panels/ ディレクトリ新設・PreferencesPanel 移動・MacroKnobPanel リネーム・ModulatablePlugin 確立 | 110 |
| Day38 | 4/2 | cc-mapping.spec.md 更新・ccMapService 実装・cc-map.json 自動生成 | 110 |
| Day39 | 4/3 | CLAUDE.md v11・handover 一元化・CLAUDE.md アーカイブシステム・NFC 正規化スクリプト | 104 |
| Day40 | 4/6 | Camera System 確認・engine.setAutoRotate() 追加 | 110 |
| Day41 | 4/7 | 壁打ち：MacroKnob D&D アサイン仕様確定・MIDI 2.0 内部バス統一・CC Map 3層構造・macro-knob.spec.md 大幅更新 | 110 |
| Day42 | 4/7 | 壁打ち：MIDI IPC 廃止確定・Web MIDI API を App.tsx で直接受信 | 110 |
| Day43 | 4/7 | ccMapService.init() → engine.initialize() 接続・App.tsx MIDI 受信 useEffect 実装 | 112 |
| Day44 | 4/7 | 壁打ち：Camera Plugin 設計確定（独立 Plugin 化・isCameraUserOverridden）・camera-plugin.spec.md 新設 | 112 |
| Day45 | 4/7 | Camera Plugin 実装完了（OrbitCamera / AerialCamera / StaticCamera・layerManager 全面移行） | 112 |
| Day46 | 4/8 | requiresRebuild フラグ実装（メッシュ再構築・全 7 Geometry Plugin 対応） | 114 |
| Day47 | 4/8 | Preferences Panel 全面改修（Geometry ドロップダウン化・Camera セクション新設・Preset システム新設） | 114 |
| Day48 | 4/8 | handover/_archive/ 廃止・Camera 初期値バグ修正 | 114 |
| Day49 | 4/8 | 壁打ち：MidiManager 責務分離確定・MacroKnobManager から handleMidiCC / receiveModulation を移管 | 110 |
| Day50 | 4/8 | MidiManager 新設（src/core/midiManager.ts）・MacroKnobManager からの責務移管完了 | 110 |
| Day51 | 4/8 | flushParameterStore() 実装・ccMapService 統合・ParameterStore ベースのパラメーター反映 | 110 |
| Day52 | 4/9 | rangeMin/rangeMax 追加・DragPayload 拡張・GeometrySimpleWindow D&D UI 実装 | 110 |
| Day53 | 4/9 | MIDIRegistry 新設（src/types/midi-registry.ts）・App.tsx Orchestrator 化・getAllLayerPlugins() | 112 |
| Day54〜55 | 4/9 | （Day53 連続作業）SimpleWindowPlugin / FxWindowPlugin Phase A 実装 | 112 |
| Day56 | 4/10 | SimpleWindowPlugin / FxWindowPlugin Phase B 完了（ccNumber 付与・value 追加）・MIDIRegistry 接続 | 112 |
| Day57 | 4/11 | Slot 概念導入（cc → slot）・Registry に value 追加・逆流 200ms ポーリング実装・transport-architecture.spec.md 新規作成 | 114 |
| **Day58** | **4/12** | **TransportEvent rename（Step1）・MidiInputWrapper 切り出し（Step2）・イベント駆動化（Step3）・ドキュメント全更新・要件定義書 v2.0・実装計画書 v4.0** | **114** |

---

## 4. 確立済みアーキテクチャ原則

### 4.1 engine.ts の制約（MUST）

- `engine.ts` は `App.tsx` に依存してはいけない・単体で動作できること
- Parameter Store の変更は必ず Command 経由のみ（直接代入禁止）
- Plugin には `renderer`・`enabled` フィールドを持たせること

### 4.2 Plugin 二分類（Day37 確立）

```
ModulatablePlugin（MIDI 2.0 / MacroKnob 制御可能）
  継承: GeometryPlugin / FXPlugin / ParticlePlugin / LightPlugin / CameraPlugin / SequencerPlugin（予定）

PluginBase のみ（外部制御不要）
  継承: TransitionPlugin / WindowPlugin / MixerPlugin
```

### 4.3 Transport Architecture（Day58 確立）

```
【外側の世界】プロトコル・セマンティクス
  cc-mapping.md（人間・AI が読む）
  MidiInputWrapper（MIDI → TransportEvent に変換）
  将来: OscInputWrapper / AI Layer（自然言語 → TransportEvent）

【境界線】
  TransportEvent { slot, value, source?, time? }

【コア層】純粋な値の処理
  TransportManager（slot + value を受け取るだけ）
  ParameterStore（slot番号をキーとして保持）
  Engine（CC番号の意味を知らない）
  TransportRegistry（slot→paramId の対応表）

【UI 層】鏡
  App.tsx（TransportRegistry を購読するだけ）
  WindowPlugin（availableParameters を filter するだけ）
```

**全入力経路の統一:**
```typescript
// 全ての入力源が同じ入り口を通る
engine.handleMidiCC(TransportEvent)
```

**Step 4（未完）: engine の ccMapService 依存解消**
- `engine.ts` の `flushParameterStore()` が `ccMapService` を直接参照している
- これが解消されると「外側と内側の境界線」が完全に閉じる
- 影響範囲が大きいため単独セッションで設計してから実装

### 4.4 MIDI 設計原則（Day42〜58 確立）

- **外部コントローラー受信**: Web MIDI API → `MidiInputWrapper` → `engine.handleMidiCC(TransportEvent)`
- **内部バス**: `TransportEvent { slot, value: 0.0〜1.0 }` で統一（プロトコル非依存）
- **MidiInputWrapper**: `src/drivers/input/MidiInputWrapper.ts`・シングルトン `midiInputWrapper`

### 4.5 Camera Plugin 設計原則（Day44〜45 確立）

- Camera は **独立した Plugin**（Geometry に埋め込まない）
- レイヤーごとに独立したインスタンス（`getCameraPlugin()` がファクトリ呼び出し）
- `isCameraUserOverridden` フラグで自動連動とユーザー意図を両立

### 4.6 Preset 設計原則（Day47 確立）

- Preset = `GeoGraphyProject` をそのまま保存
- 保存先: `localStorage`（キー: `geography:presets-v1`）
- Load → 即 engine 反映（APPLY 不要）

### 4.7 requiresRebuild フラグ（Day46 確立）

- メッシュの頂点数・形状が変わる param には `requiresRebuild: true` を必ず設定
- `engine.setGeometryParam()` が検知して `layerManager.rebuildPlugin()` を呼ぶ（destroy → create）

### 4.8 Clock の2系統（MUST・混同禁止）

```typescript
engine.threeClock  // THREE.Clock → getDelta() でフレーム間経過時間
engine.clock       // Clock（独自）→ BPM・beat 値管理・readonly で外部公開
```

### 4.9 FX 実装ルール（MUST）

- `createFxPlugins()` ファクトリを使う（シングルトン共有禁止）
- `fxStack.update()` は `enabled=false` でもスキップしない（`pass.enabled` で制御）
- FX スタック順序は `FX_STACK_ORDER` 定数に従う

### 4.10 Window / Panel 命名原則（Day28 確立）

- **Window** = Plugin エコシステムの UI・コントリビューターがデザインできる
- **Panel** = アプリ固定の小窓（Preferences Panel など）・コントリビューターが触れない
- **Simple Window** = 各 Plugin に付属するデフォルト最小 UI・カスタム Window Plugin がないときのフォールバック

---

## 5. Phase 16：Step 4 / Engine ccMapService 依存解消（現在地）

### 5.1 問題の概要

`engine.ts` の `flushParameterStore()` が `ccMapService.getCcNumber()` を直接呼んでいる。これにより「CC番号という MIDI の概念」が engine 内部に残存している。

### 5.2 目標

```
engine は TransportEvent（slot + value）だけを知る
cc-map.json の意味解釈は完全に外側（Registry / Input 層）の責務
```

### 5.3 設計の本質

```
【外側】 cc-mapping.md → cc-map.json → TransportRegistry（slot→paramId の対応）
【境界線】 TransportEvent { slot, value }
【内側】 engine は slot と value だけで動く
```

**⚠️ 実装は単独セッションで壁打ちしてから。影響範囲が大きい。**

---

## 6. Phase 17：既存 SimpleWindow 廃止（Phase 16 完了後）

廃止対象:

| 廃止対象 | 代替 |
|---|---|
| `GeometrySimpleWindow.tsx` | `SimpleWindowPlugin` |
| `FxSimpleWindow.tsx` | `FxWindowPlugin` |
| `CameraSimpleWindow.tsx` | `SimpleWindowPlugin` |

条件: SimpleWindowPlugin / FxWindowPlugin の動作確認完了後。

---

## 7. Phase 18：MacroKnob D&D アサイン UI 完成

| 作業 | 仕様参照 |
|---|---|
| Simple Window に `[≡]` D&D ハンドル追加（Geometry / Camera / FX 各 param 行） | macro-knob.spec.md §4 |
| MacroKnob へのドロップ時ダイアログ（min/max 設定） | macro-knob.spec.md §4-3 |
| 右クリックでアサイン解除（個別・全解除） | macro-knob.spec.md §4-3 |
| MacroKnob 弧インジケーター（min/max 範囲を色分け表示） | macro-knob.spec.md §4-2 |
| アサイン永続化（GeoGraphyProject に macroKnobAssigns を保存） | macro-knob.spec.md §5 |

---

## 8. Phase 19：[L1][L2][L3] タブ切り替え

- `App.tsx` に `activeLayer` state を追加
- 現在 `layer-1` 固定 → 動的切り替え

---

## 9. Phase 20：Sequencer Plugin（Phase 18 完了後）

- `docs/spec/sequencer.spec.md` が未作成 → まず**壁打ちで設計から**
- Graffiti shader の `uProgress` 制御が目的
- Shader Plugin の prerequisite

**⚠️ 実装は Phase 18 完了後。**

---

## 10. Phase 21：Shader Plugin（Phase 20 完了後）

- `ShaderPlugin extends ModulatablePlugin`
- `create(scene)` / `update(delta, beat)` / `destroy(scene)` + `params.uProgress`（必須）
- spec: `docs/spec/shader-plugin.spec.md`

**⚠️ 実装は Phase 20（Sequencer）完了後。**

---

## 11. spec ファイル一覧（Day58 時点）

| ファイル | 内容 | 状態 |
|---|---|---|
| `SDD-OVERVIEW.md` | SDD 全体の設計思想 | ✅ |
| `agent-roles.md` | マルチエージェント担当範囲 | ✅ |
| `command-pattern.spec.md` | Command パターン | ✅ 実装済み |
| `plugin-registry.spec.md` | Plugin Registry | ✅ 実装済み |
| `program-preview-bus.spec.md` | Program/Preview バス | ✅ 実装済み |
| `transition-plugin.spec.md` | Transition Plugin | ✅ 実装済み |
| `mixer-plugin.spec.md` | MixerPlugin Interface | ✅ 実装済み |
| `simple-window.spec.md` | Simple Window の概念 | ✅ 実装済み |
| `window-plugin.spec.md` | Window Plugin 再定義 | ✅ 実装済み |
| `layer-system.spec.md` | レイヤーシステム | ✅ 実装済み |
| `macro-knob.spec.md` | マクロノブ（D&D アサイン含む） | ✅ 仕様確定 |
| `fx-stack.spec.md` | FX スタック | ✅ 実装済み |
| `geometry-plugin.spec.md` | Geometry Plugin 共通 | ✅ 実装済み |
| `camera-plugin.spec.md` | Camera Plugin（独立 Plugin 化） | ✅ 実装済み |
| `electron.spec.md` | Electron アーキテクチャ | ✅ 実装済み |
| `preferences-panel.spec.md` | Preferences Panel | ✅ 実装済み |
| `project-file.spec.md` | プロジェクトファイル | ✅ 実装済み |
| `cc-standard.spec.md` | CC Standard（Block 1xx〜8xx） | ✅ v0.2 |
| `cc-mapping.spec.md` | CC マッピング・ccMapService | ✅ 実装済み |
| `midi-registry.spec.md` | MIDIRegistry（動的状態管理） | ✅ Day57 確定 |
| `plugin-manager.spec.md` | Plugin Manager（Phase A/B） | ✅ Day57 実装完了 |
| `transport-architecture.spec.md` | Transport Architecture（Step1〜4） | ✅ Day57 作成・Day58 Step1〜3 実装済み |
| `shader-plugin.spec.md` | Shader Plugin | ✅ 設計済み・実装は Phase 21 |
| `plugin-lifecycle.spec.md` | Plugin ライフサイクル | ⬜ 未着手 |
| `sequencer.spec.md` | Sequencer Plugin | ⬜ 未作成（壁打ちから） |

---

## 12. ハマりポイント集（Day1〜58 の知見）

| 問題 | 原因 | 解決策 |
|---|---|---|
| FX ON/OFF が効かない | `fxStack.update()` で enabled=false をスキップ | `update()` は常に全 Plugin を呼ぶ・`pass.enabled` で制御 |
| 複数レイヤーで FX クラッシュ | `getAllFxPlugins()` シングルトンを共有 | `createFxPlugins()` ファクトリで毎回新インスタンス生成 |
| `IFxStack` 循環参照 | `types/index.ts` から `core/fxStack.ts` を import | `IFxStack` インターフェースを `types/index.ts` 内に定義 |
| preload.js が ES Module エラー | ルートに `"type": "module"` があるため | `electron/package.json` に `{ "type": "commonjs" }` を追加 |
| isDev 判定が誤る | `process.env.NODE_ENV` は Electron ビルドで信頼できない | `!app.isPackaged` を使う |
| beat 値が急ジャンプ | `setTempo()` 時に `startTime` をリセットしていない | テンポ変更時に `startTime = performance.now()` でリセット |
| drawImage で WebGL canvas が真っ黒 | WebGL のデフォルトは描画後バッファをクリア | `THREE.WebGLRenderer` に `preserveDrawingBuffer: true` を追加 |
| edit_file の oldText がマッチしない | macOS APFS が日本語を NFD で保存するが Claude は NFC で送る | `python3 /Users/shinbigan/nfc_normalize.py` で NFC 正規化 |
| Camera 初期値が常に Static になる | PreferencesPanel の useEffect が resolveCamId を使っていた | engine.getLayers() から cameraPlugin.id を直接読む（Day48修正済み） |
| Camera Plugin が複数レイヤーで状態干渉 | ファクトリを呼ばずシングルトンを共有 | `getCameraPlugin()` がファクトリ呼び出しで毎回独立インスタンス |
| Geometry param 変更で見た目が変わらない | segments 等はメッシュ再構築が必要 | `requiresRebuild: true` フラグ + `rebuildPlugin()` で destroy→create |
| MidiInputWrapper を App.tsx に直書きしていた | 関心の分離ができていなかった | `src/drivers/input/MidiInputWrapper.ts` に切り出し（Day58 Step2） |
| 200ms ポーリングで Plugin 数増加時に負荷増大 | 変化がなくても毎回全走査 | `engine.onParamChanged` コールバック + 16ms throttle に変更（Day58 Step3） |
| zsh でインラインコメント（#）がエラー | zsh のインタラクティブモードはコメント非対応 | コメント行とコマンド行を分けて渡す |

---

## 13. v2 実装計画

| 順序 | 内容 | 種別 |
|---|---|---|
| 1 | Geometry Launcher | MixerPlugin の一種・自動 VJ |
| 2 | MixerPlugin 本格 Plugin 化 | アーキテクチャ |
| 3 | CrossfadeMixer | MixerPlugin |
| 4 | GL Transitions 移植（厳選10種） | Transition Plugin |
| 5 | Embed Export | Output |
| 6 | PixiJS Plugin Group | Plugin Group |

---

## 14. ロードマップ

| バージョン | 主な内容 | 状態 |
|---|---|---|
| v1 | Three.js 基盤・Plugin/Driver・Electron・Simple Window 体系・Program/Preview バス・Beat Cut・マクロノブ D&D・Transport Architecture・Sequencer・Shader Plugin・録画・OSS 公開 | 進行中 |
| v2 | Geometry Launcher・CrossfadeMixer・GL Transitions・Embed Export・PixiJS Plugin Group | 未定 |
| v3 | Full Morph・opentype.js Typography・Spatial Extension | 未定 |
| v4 | Claude.ai 自動トランジション・Video Input Driver（Resolume 受信） | 未定 |

--- 以上 ---
