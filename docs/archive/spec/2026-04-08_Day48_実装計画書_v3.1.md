# GeoGraphy 実装計画書 v3.1

> 要件定義書 v1.9 に基づく実装計画・開発フロー・Day 実績記録

| 項目 | 内容 |
|---|---|
| アプリ名 | GeoGraphy |
| 実装計画書バージョン | 3.1.0 |
| 対応要件定義書 | v1.9 |
| 作成日 | 2026年3月（Day28壁打ち反映） |
| 前バージョン | v3.0 |
| 実行環境 | Mac / Node.js v25 / Vite / TypeScript / pnpm v10.32+ / Electron 41 |
| レンダラー | Three.js r160+ |
| 開発スタイル | SDD × CDD（仕様駆動 × コンパイラ駆動） |

---

## 1. 開発の基本方針

### 1.1 SDD × CDD 原則

- **SDD（Spec-Driven Development）**: 実装前に必ず `docs/spec/` の spec ファイルを読む。仕様変更はコードより先に spec を修正する
- **CDD（Compiler-Driven Development）**: 完了条件は `pnpm tsc --noEmit`（型エラーゼロ）+ `pnpm test --run`（全テストグリーン）の両方通過
- **`any` 禁止**: 型エラーは自律修正する
- **プラン先行**: 実装前にプランを提示・承認を得てから実装開始

### 1.2 開発サイクル

```
1. docs/spec/[機能].spec.md を確認（または新規作成）
2. プランを提示・承認を得てから実装開始
3. 実装 → pnpm tsc --noEmit → 型エラーがあれば自律修正
4. pnpm test --run → 全グリーンになるまでループ
5. docs/progress/[task].log.md に完了を記録
6. セッション終了時に git commit + Day タグ（git tag dayN && git push origin dayN）
7. HANDOVER.md を更新
8. Obsidian dev-log に記録
```

### 1.3 ツール役割分担

| ツール | 役割 |
|---|---|
| Claude Desktop | spec 制作・要件定義・実装計画・CLAUDE.md 更新・壁打ち |
| Claude Code | spec を読んでから実装・tsc + test 通過・Git 操作 |
| Obsidian（GeoGraphy Vault） | 開発日誌・意思決定記録・YouTube 素材 |

### 1.4 CLAUDE.md の階層構造

| CLAUDE.md の場所 | 内容 | 状態 |
|---|---|---|
| geography/CLAUDE.md | プロジェクト全体方針・SDD×CDD 原則・MUST ルール | 要更新（v10） |
| docs/spec/ | SSoT（仕様ファイル群）・マルチエージェント定義 | 要更新・新規追加 |
| src/core/CLAUDE.md | エンジン・Clock・LayerManager・Bus 設計 | 要更新 |
| src/plugins/geometry/CLAUDE.md | renderer・enabled・cameraPreset | 現状維持 |
| src/plugins/transitions/CLAUDE.md | UI を持たない・execute() 純粋関数 | 現状維持 |
| src/plugins/mixers/CLAUDE.md | MixerPlugin の定義・Simple Window との関係 | **新規作成** |
| src/plugins/windows/CLAUDE.md | Window Plugin 再定義・データ渡し先の宣言 | 要更新 |
| src/plugins/fx/CLAUDE.md | FX スタック順序・createFxPlugins() ファクトリ | 現状維持 |
| src/ui/CLAUDE.md | Simple Window 命名原則・View メニュー連携 | 要更新 |

---

## 2. 実装フェーズ全体

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | 基盤構築（Vite / React / TS / Three.js / GitHub / CI） | ✅ Day1-4 完了 |
| Phase 2 | Plugin / Driver アーキテクチャ + Interface 全定義 + Command パターン | ✅ Day5-6 完了 |
| Phase 3 | Program/Preview バス + Mixer Simple Window + Beat Cut + CrossFade | ✅ Day6-11 完了 |
| Phase 4 | レイヤーシステム + SDD 基盤（spec ディレクトリ） | ✅ Day12 完了 |
| Phase 5 | MacroKnobManager（コア）+ Macro Knob Simple Window UI | ✅ Day13-14 完了 |
| Phase 6 | FX スタック（10種）+ FX Simple Window + バグ修正 | ✅ Day15-19 完了 |
| Phase 7 | Geometry Plugin 追加 + BlendMode + Camera System | ✅ Day20-21 完了 |
| Phase 8 | Electron 化 + Preferences Panel + Plugin Lifecycle | ✅ Day22-25 完了 |
| Phase 9 | メニューバー整備 + .dmg ビルド + UI 設計見直し | ✅ Day26-27 完了 |
| Phase 10 | アーキテクチャ整理（Day28壁打ち反映） | ✅ Day29-30 完了 |
| Phase 11 | Preview サムネイルキャンバス → Small screen 実装で代替 | ✅ Day31 完了 |
| Phase 12 | Output Driver + 録画 | ⬜ 未着手 |
| Phase 13 | UI 全面実装（Window Plugin 群） | ⬜ 未着手 |
| Phase 14 | OSS 公開・v1 リリース | ⬜ 未着手 |

---

## 3. Day 別実装実績

| Day | 日付 | 主な実装内容 | テスト数 |
|---|---|---|---|
| Day1-4 | 3/18以前 | Vite + React + TS + Three.js 基盤・GitHub・CI・grid-wave plugin | - |
| Day5 | 3/18 | Obsidian 導入・engine.ts 骨格・ParameterStore テスト | 34 |
| Day6 | 3/18 | ProgramBus・PreviewBus・SimpleMixer 骨格 | 34 |
| Day7 | 3/18 | CrossFade Transition Plugin・execute() 純粋関数化・App.tsx 統合 | 34 |
| Day8 | 3/18 | SimpleMixer ↔ ProgramBus/PreviewBus 接続・プレビューキャンバス mount | 34 |
| Day9 | 3/18 | engine.ts Plugin create() 呼び出し・初期 SceneState 生成 | 34 |
| Day10 | 3/18 | BPM クロック（clock.ts）・engine.threeClock 分離・TAP ボタン | 38 |
| Day11 | 3/18 | Beat Cut → engine.ts 接続・ラップアラウンド検出 | 38 |
| Day12-pre | 3/22 | SDD 基盤（docs/spec/ 9本）・CLAUDE.md v7・マルチエージェント設計 | 38 |
| Day12 | 3/23 | LayerManager・CSS 合成・SimpleMixer PROGRAM エリア更新 | 43 |
| Day13 | 3/23 | MacroKnobManager・normalize()・engine.handleMidiCC() | 50 |
| Day14 | 3/23 | MacroKnobPanel UI（32ノブ SVG arc・EditDialog） | 50 |
| Day15 | 3/23 | FX スタック（10種）・FxStack クラス・FX_STACK_ORDER | 61 |
| Day16 | 3/23 | engine FX 統合・createFxPlugins() ファクトリ・IFxStack 循環参照回避 | 64 |
| Day17 | 3/23 | FxControlPanel UI・engine FX API（getFxPlugins/setFxEnabled/setFxParam） | 71 |
| Day18 | 3/23 | FX ON/OFF バグ修正（update スキップ問題・レイヤー配置問題） | 71 |
| Day19 | 3/23 | FX 小数表示バグ修正・UI ショートカット（1/2/3/H/S/F/P） | 71 |
| Day20 | 3/23 | BlendMode セレクター・Contour plugin・Grid Tunnel plugin | 85 |
| Day21 | 3/23 | Camera System（CameraPreset・自動適用）・Opacity スライダー | 90 |
| Day22 | 3/23 | CLAUDE.md v8・spec 3本（electron/preferences/project-file）・壁打ち | 90 |
| Day23 | 3/23 | PreferencesPanel（Setup/Project タブ）・Plugin Lifecycle（applySetup） | 90 |
| Day24 | 3/24 | Electron Phase 1（main.js/preload.js）・geoAPI 型定義・IPC セキュリティ | 90 |
| Day25 | 3/24 | autosave（終了時保存・起動時復元）・Project File・Setup Geometry 反映 | 104 |
| Day26 | 3/24 | （詳細ログなし） | 104 |
| Day27 | 3/24 | isDev 修正・.dmg ビルド確認・メニューバー整備・⚙ボタン廃止・.gitignore 修正 | 104 |
| Day28 | 3/24 | 壁打ち：アーキテクチャ整理・要件定義書 v1.9・実装計画書 v3.1 | 104 |
| Day29 | 3/25 | Phase 10 実装：mixers/ 新設・ファイルリネーム・View メニュー・CLAUDE.md 更新 | 104 |
| Day30 | 3/25 | Phase 10 仕上げ：spec 更新・CLAUDE.md v10・バグ修正 | 104 |
| Day31 | 3/26 | Small screen 実装（drawImage・200msポーリング・preserveDrawingBuffer・DPR対応） | 104 |

---

## 4. 確立済みアーキテクチャ原則

### 4.1 engine.ts の制約（MUST）

- `engine.ts` は `App.tsx` に依存してはいけない・単体で動作できること
- Parameter Store の変更は必ず Command 経由のみ（直接代入禁止）
- Plugin には `renderer`・`enabled` フィールドを持たせること

### 4.2 Clock の2系統（MUST・混同禁止）

```typescript
engine.threeClock  // THREE.Clock → getDelta() でフレーム間経過時間
engine.clock       // Clock（独自）→ BPM・beat 値管理・readonly で外部公開
```

### 4.3 FX 実装ルール（MUST）

- `createFxPlugins()` ファクトリを使う（シングルトン共有禁止）
- `fxStack.update()` は `enabled=false` でもスキップしない（`pass.enabled` で制御）
- FX スタック順序は `FX_STACK_ORDER` 定数に従う

### 4.4 Electron アーキテクチャ（MUST）

- Save / Load / Recent / メニュー定義 → `main.js` に集約
- engine 操作 / Web MIDI / autosave → レンダラー（React）
- Output タブ実装時は最初から IPC 経由で設計する
- `isDev = !app.isPackaged`（`process.env.NODE_ENV` は使わない）

### 4.5 Window / Panel 命名原則（Day28 確立）

- **Window** = Plugin エコシステムの UI・コントリビューターがデザインできる
- **Panel** = アプリ固定の小窓（Preferences Panel など）・コントリビューターが触れない
- **Simple Window** = 各 Plugin に付属するデフォルト最小 UI・カスタム Window Plugin がないときのフォールバック
- すべての Window は View メニューから表示/非表示を切り替えられる

### 4.6 MixerPlugin（Day28 確立）

- **MixerPlugin** = コントリビューターが開発・登録する Plugin 単位
- **Mixer Simple Window** = MixerPlugin 起動時のデフォルト最小 UI
- Geometry Launcher は MixerPlugin の一種（自動化された Mixer）
- v1 は Mixer Simple Window 固定・MixerPlugin の交換は v2 から

### 4.7 Window Plugin（Day28 再定義）

- 各 Plugin に対してカスタム UI を差し込める仕組み
- 事前にどの Plugin にデータを渡すかを宣言して設計する
- カスタム Window Plugin が有効なとき → 対応する Simple Window は非表示
- v2 から本格導入

### 4.8 Transition Plugin（MUST）

- `execute()` は純粋関数として実装（副作用なし・戻り値は `SceneState`）
- CrossFade の category は `'parameter'`

---

## 5. Phase 10：アーキテクチャ整理（Day28壁打ち反映）

Day28 の壁打ちで確定したアーキテクチャ変更を実装に反映する。**tsc + test 両方通過が完了条件。**

### 5.1 ディレクトリ変更

| 作業 | 内容 |
|---|---|
| `src/plugins/mixers/` 新規作成 | MixerPlugin 専用ディレクトリ |
| `src/plugins/windows/simple-mixer/` → `src/plugins/mixers/simple-mixer/` | 移動 |
| `src/plugins/windows/` | Window Plugin（カスタムUI）置き場として再定義・中身は空に |

### 5.2 ファイルリネーム

| 現在のファイル名 | 新しいファイル名 | 場所 |
|---|---|---|
| `SimpleMixer.tsx` | `MixerSimpleWindow.tsx` | `src/plugins/mixers/simple-mixer/` |
| `FxControlPanel.tsx` | `FxSimpleWindow.tsx` | `src/ui/` |
| `MacroKnobPanel.tsx` | `MacroKnobSimpleWindow.tsx` | `src/ui/` |

### 5.3 メニューバー変更

| 作業 | 内容 |
|---|---|
| `electron/main.js` に View メニューを追加 | 全 Window の表示/非表示トグル |
| `electron/preload.js` に View メニューイベントを追加 | `onMenuEvents` 拡張 |
| `src/types/geoAPI.d.ts` に型定義を追加 | View メニューイベントの型 |

### 5.4 CLAUDE.md 更新・新規作成

| ファイル | 作業 |
|---|---|
| `geography/CLAUDE.md` | v10 に更新（Window/Panel 原則・Simple Window・View メニュー） |
| `src/core/CLAUDE.md` | エンジン固定部分の定義を更新（MacroKnobManager を明記） |
| `src/plugins/mixers/CLAUDE.md` | **新規作成**（MixerPlugin の定義・Simple Window との関係） |
| `src/plugins/windows/CLAUDE.md` | Window Plugin 再定義に更新 |
| `src/ui/CLAUDE.md` | Simple Window 命名原則・View メニュー連携を追記 |

### 5.5 spec ファイル更新・新規作成

| ファイル | 作業 |
|---|---|
| `docs/spec/mixer-plugin.spec.md` | MixerPlugin 再定義・Simple Window との関係 |
| `docs/spec/simple-window.spec.md` | **新規作成**（Simple Window の概念・フォールバック動作） |
| `docs/spec/window-plugin.spec.md` | **新規作成**（Window Plugin 再定義・データ渡し先の宣言） |
| `docs/spec/electron.spec.md` | View メニューの追加 |

### 5.6 実装順序

```
1. spec ファイルを更新・新規作成（SDD 原則）
2. src/plugins/mixers/ ディレクトリ新規作成
3. simple-mixer を移動・リネーム
4. FxSimpleWindow.tsx・MacroKnobSimpleWindow.tsx リネーム
5. App.tsx の import を全て更新
6. electron/main.js に View メニュー追加
7. CLAUDE.md 群を更新・新規作成
8. pnpm tsc --noEmit → 型エラーゼロ確認
9. pnpm test --run → 全テストグリーン確認
10. git commit + tag day28
```

---

## 6. Phase 11：Small screen 実装（Day31 完了）

### 6.1 実装内容

Preview サムネイルキャンバス（専用 WebGL レンダラー方式）は **Small screen 実装で代替**された。

| 項目 | 実装内容 |
|---|---|
| 表示方式 | `layerManager` の各レイヤー canvas を `drawImage` で 200ms ポーリング合成 |
| サイズ | 240×135px（CSS）/ DPR 対応で物理ピクセルは 480×270（Retina） |
| 配置 | MixerSimpleWindow の左側・フェーダーエリアと横並び |
| ソース | `screenAssign.small` が `edit` → editOpacity / `output` → outputOpacity でブレンド |

### 6.2 確立した設計原則

- Small screen = Large screen と**別の映像**を映す（SWAP で役割が入れ替わる）
- `THREE.WebGLRenderer` に `preserveDrawingBuffer: true` が必須（`layerManager.ts` に追加済み）
- 200ms ポーリング方式は Three.js レンダーループに干渉しない軽量設計
- v2 で専用 WebGL レンダラー方式を検討（現状は drawImage の限界で約 5fps）

---

## 7. Phase 12：Output Driver + 録画

| 作業 | 内容 |
|---|---|
| canvas.captureStream(60) | MediaRecorder で WebM 録画 |
| File メニュー連携 | Start Recording / Stop Recording を main.js 経由で |
| 録画中表示 | メニューバーに赤い ● |
| NDI 出力 | OBS 経由 |

**注意**: ファイル書き出しは最初から main.js 経由（IPC）で設計すること。

---

## 8. Phase 13：UI 全面実装

| Window | 主な実装内容 | 種別 |
|---|---|---|
| Geometry Simple Window | SHAPE / COLOR（Hue・Alpha）/ RECOMMENDED FX / LIGHT / AUTO / MIDI MAPPING | Simple Window |
| Camera Simple Window | POSITION / ROTATION / AUTO / MIDI MAPPING | Simple Window |
| FX Simple Window | 全 FX リスト + ColorGrading + AUTO + MIDI MAPPING（既存を拡充） | Simple Window |
| Layer Simple Window | レイヤー追加削除・opacity・blendMode・GPU 使用率・FPS | Simple Window |
| Preset Simple Window | プリセット一覧・フォルダ管理・New / Import / Export | Simple Window |
| Tempo Simple Window | BPM・Ableton Link 状態・Tap / Reset | Simple Window |
| Preferences Panel | MIDI / Output / Tempo / Camera / Visual Defaults / Modulator タブ | Panel（固定） |

---

## 9. v2 実装計画

### 9.1 v2 実装順序

| 順序 | 内容 | 種別 | 理由 |
|---|---|---|---|
| 1 | Preview サムネイル描画の完成・強化 | Phase 11 | v1 の締めくくり |
| 2 | Geometry Launcher | MixerPlugin の一種 | 自動 VJ・すぐ現場で使える |
| 3 | MixerPlugin 本格 Plugin 化 | アーキテクチャ | v3 以降のコントリビューターのため |
| 4 | CrossfadeMixer | MixerPlugin | 本格 VJ 向け |
| 5 | Geometry Sequencer | Window Plugin | 手動／自動2モード・実験場 |
| 6 | GL Transitions 移植（厳選10種） | Transition Plugin | Transition の選択肢を増やす |
| 7 | Embed Export | Output | Web 展開・スクリーンセーバー対応 |
| 8 | PixiJS Plugin Group | Plugin Group | 2D 塗り潰し図形・新しい表現 |

### 9.2 Geometry Launcher（MixerPlugin の一種）

自動化された MixerPlugin。手動／自動の2モードを持つ。

```typescript
// 自動モード
const playlist: SceneState[] = [sceneA, sceneB, sceneC]
let current = 0

onBeat(4, () => {
  previewBus.setState(playlist[current])
  mixer.triggerCrossfade(selectedTransition)
  current = (current + 1) % playlist.length
})
```

### 9.3 Geometry Sequencer（Window Plugin）

Geometry Plugin へのカスタム操作 UI。手動／自動の2モードを持つ。

- **手動モード**: パラメーターを時系列で編集・確認する UI
- **自動モード**: BPM に合わせてパラメーターを自動変調
- Tempo Driver・Modulator Driver と深く統合

---

## 10. ハマりポイント集（Day1〜28 の知見）

| 問題 | 原因 | 解決策 |
|---|---|---|
| FX ON/OFF が効かない | `fxStack.update()` で enabled=false をスキップ | `update()` は常に全 Plugin を呼ぶ・`pass.enabled` で制御 |
| 複数レイヤーで FX クラッシュ | `getAllFxPlugins()` シングルトンを共有 | `createFxPlugins()` ファクトリで毎回新インスタンス生成 |
| `IFxStack` 循環参照 | `types/index.ts` から `core/fxStack.ts` を import | `IFxStack` インターフェースを `types/index.ts` 内に定義 |
| App.tsx が engine を呼んでいなかった | 旧 App.tsx が独自 Scene/Renderer を管理 | `engine.initialize(container)` → `engine.start()` のみに変更 |
| preload.js が ES Module エラー | ルートに `"type": "module"` があるため | `electron/package.json` に `{ "type": "commonjs" }` を追加 |
| isDev 判定が誤る | `process.env.NODE_ENV` は Electron ビルドで信頼できない | `!app.isPackaged` を使う |
| `dist-electron/` が誤コミット | `.gitignore` に未追加 | `.gitignore` に追加・`git rm -r --cached` で追跡解除 |
| zsh が `!app.isPackaged` をエラー扱い | zsh の history expansion | filesystem ツールで直接ファイル編集 |
| beat 値が急ジャンプ | `setTempo()` 時に `startTime` をリセットしていない | テンポ変更時に `startTime = performance.now()` でリセット |
| THREE.Clock と BPM Clock の命名衝突 | 同じ名前で管理していた | `threeClock`（delta 用）と `clock`（BPM 用）に明確分離 |
| GeoGraphyProject の layers フィールド不整合 | `sceneState.layers` / `setup` / `version` の構造を誤解 | 正しい構造で初期化 |

---

## 11. spec ファイル一覧（現状）

| ファイル | 内容 | 状態 |
|---|---|---|
| `SDD-OVERVIEW.md` | SDD 全体の設計思想 | ✅ |
| `agent-roles.md` | マルチエージェント担当範囲 | ✅ |
| `command-pattern.spec.md` | Command パターン | ✅ 実装済み |
| `plugin-registry.spec.md` | Plugin Registry | ✅ 実装済み |
| `program-preview-bus.spec.md` | Program/Preview バス | ✅ 実装済み |
| `transition-plugin.spec.md` | Transition Plugin | ✅ 実装済み |
| `mixer-plugin.spec.md` | MixerPlugin Interface | 🔴 要更新（Day28反映） |
| `layer-system.spec.md` | レイヤーシステム | ✅ Day12 実装済み |
| `macro-knob.spec.md` | マクロノブ | ✅ Day13-14 実装済み |
| `fx-stack.spec.md` | FX スタック | ✅ Day15-18 実装済み |
| `fx-control-ui.spec.md` | FX コントロール UI | 🔴 要更新（FX Simple Window にリネーム） |
| `geometry-plugin.spec.md` | Geometry Plugin 共通 | ✅（v1 未着手分あり） |
| `camera-system.spec.md` | カメラシステム | ✅ Day21 実装済み |
| `electron.spec.md` | Electron アーキテクチャ | 🔴 要更新（View メニュー追加） |
| `preferences-panel.spec.md` | Preferences Panel | ✅ Day23 実装済み |
| `project-file.spec.md` | プロジェクトファイル | ✅ Day25 実装済み |
| `plugin-lifecycle.spec.md` | Plugin ライフサイクル | ⬜ 未着手 |
| `simple-window.spec.md` | Simple Window の概念 | 🔴 **新規作成** |
| `window-plugin.spec.md` | Window Plugin 再定義 | 🔴 **新規作成** |

---

## 12. 注意点・矛盾点の整理

| 懸念点 | 解決方針 |
|---|---|
| Phase 10 のリネーム・移動で import パスが大量に変わる | App.tsx を起点に一括修正・tsc で検出 |
| Mixer Simple Window の「閉じてはいけない」制約は旧コードに残っている可能性 | Phase 10 でコードレビュー・削除 |
| `src/plugins/windows/CLAUDE.md` が旧定義のまま残っている | Phase 10 で更新 |
| Preview サムネイルの FPS 影響 | Phase 11 実装後に必ず FPS を計測・問題があれば描画頻度を下げる |
| GL Transitions の移植コスト | v2 では厳選10種のみ移植・コントリビューターに開放して増やす |
| Output タブの将来実装 | 最初から main.js 経由（IPC）で設計する |

---

## 13. OSS 運営

| 種類 | 場所 | 追加方法 | 必要なファイル |
|---|---|---|---|
| Geometry Plugin | plugins/geometry/ | CLI で生成 → PR | index.ts / Geometry.ts / config.ts / CLAUDE.md / README.md / template-basic.md / template-all.md |
| Particle Plugin | plugins/particles/ | CLI で生成 → PR | 同上 |
| FX Plugin | plugins/fx/ | CLI で生成 → PR | 同上 |
| Light Plugin | plugins/lights/ | CLI で生成 → PR | 同上 |
| MixerPlugin | plugins/mixers/ | React FC を実装 → PR | index.ts / MixerSimpleWindow.tsx / CLAUDE.md / README.md |
| Transition Plugin | plugins/transitions/ | CLI で生成 → PR | index.ts / CLAUDE.md / README.md |
| Window Plugin | plugins/windows/ | React FC を実装・データ渡し先を宣言 → PR | index.ts / Window.tsx / CLAUDE.md / README.md |
| 2D Plugin（v2） | plugins/2d/ | CLI で生成 → PR | 同上 |
| Typography Plugin（v3） | plugins/typography/ | CLI で生成 → PR | 同上 |
| MIDI マッピング | settings/mappings/midi/ | .md を作成 → PR | [controller].md |

---

## 14. YouTube コンテンツ計画

### シリーズ 1：開発ドキュメンタリー

| # | タイトル |
|---|---|
| 01 | 要件定義・アーキテクチャ設計 |
| 02 | Three.js 基盤構築 |
| 03 | Plugin System 実装 |
| 04 | grid-wave + starfield プラグイン制作 |
| 05 | Camera システム実装 |
| 06 | マクロノブ実装 |
| 07 | レイヤーシステム実装 |
| 08 | MIDI + Resolume 連携 |
| 09 | Electron 化・.dmg 配布 |

### シリーズ 2：プラグイン制作チュートリアル

| # | タイトル |
|---|---|
| 01 | 環境構築・CLI の使い方 |
| 02 | 初めての Geometry Plugin |
| 03 | Particle Plugin で背景を作る |
| 04 | PR の送り方 |

### シリーズ 3：Claude.ai と一緒に VJ する

| # | タイトル |
|---|---|
| 01 | 「テクノっぽくして」だけでプロ級の設定が完成する |
| 02 | ライブ中に「もっと激しく」と打ち込むだけ |
| 03 | 初心者が Claude.ai だけで 1 時間でオリジナルセットを作る |

### シリーズ 4：GeoGraphy で Web を彩る

| # | タイトル |
|---|---|
| 01 | GeoGraphy の映像をホームページの背景にする方法 |
| 02 | ポートフォリオサイトに動く背景を5分で追加する |
| 03 | Mac のスクリーンセーバーを GeoGraphy で自作する方法 |
| 04 | Squarespace / Wix への埋め込み方法 |

---

## 15. ロードマップ

| バージョン | 主な内容 | 時期 |
|---|---|---|
| v1 | Three.js 基盤・Plugin/Driver・Electron・Simple Window 体系・Program/Preview バス・Beat Cut + CrossFade・マクロノブ・レイヤー・録画・OSS 公開 | 進行中 |
| v2 | Geometry Launcher（MixerPlugin）・Geometry Sequencer（Window Plugin）・MixerPlugin 本格 Plugin 化・CrossfadeMixer・GL Transitions・Embed Export・PixiJS | 未定 |
| v3 | Full Morph・opentype.js Typography・Spatial Extension（プロジェクションマッピング） | 未定 |
| v4 | Claude.ai 自動トランジション・Video Input Driver（Resolume 受信） | 未定 |
| Browser Bridge | Syphon/Spout ユーティリティ（別プロジェクト・GeoGraphy v1 完成後） | 未定 |

--- 以上 ---
