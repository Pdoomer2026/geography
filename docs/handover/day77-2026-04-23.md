# GeoGraphy 引き継ぎメモ｜Day77｜2026-04-23

## プロジェクト概要
- アプリ名: GeoGraphy（No-Texture・Plugin駆動・映像制作プラットフォーム）
- スタック: Vite / React 18 / TypeScript / Three.js r160+ / pnpm / Electron 41 / RxJS / Zustand / Zod / Tone.js
- 開発スタイル: SDD × CDD（spec 先行 → tsc + test 両通過）
- GitHub: https://github.com/Pdoomer2026/geography
- ブランチ: `refactor/day53-design`
- 直近タグ: `day74`（day75〜77 は未コミット・未タグ）

## 重要ファイルパス

| ファイル | パス |
|---|---|
| 要件定義書 v2.1 | `/Users/shinbigan/geography/docs/要件定義書_v2.1.md` |
| 実装計画書（アーカイブ済） | `/Users/shinbigan/geography/docs/archive/2026-04-23_Day77_実装計画書_v4.0.md` |
| HANDOVER.md | `/Users/shinbigan/geography/HANDOVER.md` |
| main.js | `/Users/shinbigan/geography/electron/main.js` |
| App.tsx | `/Users/shinbigan/geography/src/ui/App.tsx` |
| projectManager | `/Users/shinbigan/geography/src/application/adapter/storage/projectManager.ts` |

## 今回のセッションで完了したこと

- 要件定義書 v2.0 → アーカイブ（`docs/archive/2026-04-23_Day77_要件定義書_v2.0.md`）
- 要件定義書 v2.1 新規作成（Day59〜76 の変更を全反映）
  - PixiJS 完全削除
  - opentype.js / GL Transitions → v1 前倒し
  - MixerPlugin / Geometry Launcher → v1 前倒し
  - OSS 公開 → v2 に戻す
  - IAC 追加・YouTube 圧縮・SDK/WASM §39/40 新設
  - §3.3/3.4/§4 に「⚠️ 要確認」マーク追加
- 実装計画書 v4.0 → アーカイブ（新規未作成）
- main.js / App.tsx の実装照合完了

## 現在の状態（重要）

**要件定義書 v2.1 の未解決箇所：**
- §3.3 Electron 固定部分：autosave / save-recording / cc-map / cc-overrides / get-data-dir の IPC ハンドラーが未記載
- §3.4 レンダラーに残すべき処理：RxJS commandStream 接続 / Zustand store 接続 / キーボードショートカット管理 / 録画ロジック / MidiMonitor コールバック管理 が未記載

**Electron 薄い鏡化 設計確定内容：**
```
やること:
  main.js をスリム化（メニュークリック → sendToRenderer() だけ）
  ファイル操作ロジックを application 層（projectManager）へ移す
  main.js は fs / dialog / IPC の純粋な橋渡しのみにする

やらないこと:
  windowMode の Zustand 移行（React state に留めて正しい）
  React メニューの変更（別サーフェス・一切触らない）

構造:
  React UI メニュー（開発・Vercel デモ用） → application 層
  Electron ネイティブメニューバー（製品・.dmg用） → application 層
  ※ 同じ application 層を参照するが2つは別サーフェス
```

## 発生した問題と解決策

- `create_file`（コンテナ内専用）を誤使用 → `filesystem:write_file` で再書き込み（トークン無駄遣い）
- NFC 正規化問題で `edit_file` アンカーマッチ失敗 → `filesystem:write_file` で全体置換
- 長いセッションで filesystem MCP ツールが消失 → 新チャットで続行

## 次回やること（優先順位順）

1. **Electron 薄い鏡化を実装**
   - `main.js` をリファクタ（メニュー → `sendToRenderer()` のみ）
   - `recentManager.ts` を `application/adapter/storage/` に新設
   - `projectManager` に Recent 管理を統合
   - tsc + test → commit → tag day77

2. **要件定義書 v2.1 の §3.3/3.4 を実装後に更新**（実装完了後に正確な内容で更新）

3. **実装計画書 v4.1 を新規作成**（要件定義書 v2.1 + Electron 薄い鏡化反映）

4. **film / frei-chen の `FX_STACK_ORDER` 未登録バグ修正**（既知の deferred item）

## 環境メモ

- `pnpm tsc --noEmit 2>&1 && pnpm test --run 2>&1` で tsc + test を同時確認
- commit は `.claude/day77-commit.txt` に書いて `git commit -F` で実行（日本語 `-m` は zsh で hang）
- `git tag day77` → `git push origin refactor/day53-design --tags`
- NFC 正規化: `python3 /Users/shinbigan/nfc_normalize.py`（日本語ファイル書き込み後に必ず実行）
- filesystem MCP が消失したら新チャットを開く
- ブラウザ確認: `open http://localhost:5173`（HMR では反映されない）
- テスト数: 129 green / tsc エラー: 0（Day76 時点）
