# GeoGraphy 引き継ぎメモ｜Day61（CLAUDE.md 全面整合・Window 体系確定）｜2026-04-15

## プロジェクト概要
- **アプリ名**: GeoGraphy（Geometry×地形×Graph のダブルミーニング）
- **目的**: No-Texture・Plugin駆動・マルチライブラリ対応の映像制作プラットフォーム
- **スタック**: Vite / React 18 / TypeScript / Three.js r160+ / pnpm v10.32+ / Electron 41
- **開発スタイル**: SDD × CDD（仕様駆動 × コンパイラ駆動）
- **GitHub**: https://github.com/Pdoomer2026/geography
- **プロジェクトルート**: `/Users/shinbigan/geography`

---

## 重要ファイルパス

| ファイル | パス |
|---|---|
| CLAUDE.md（全体方針） | `CLAUDE.md`（v11） |
| src/core/CLAUDE.md | `src/core/CLAUDE.md`（v7・Day61 更新済み） |
| src/ui/CLAUDE.md | `src/ui/CLAUDE.md`（v4・Day61 更新済み） |
| src/ui/panels/CLAUDE.md | `src/ui/panels/CLAUDE.md`（v3・Day61 更新済み） |
| src/plugins/windows/CLAUDE.md | `src/plugins/windows/CLAUDE.md`（v3・Day61 更新済み） |
| src/plugins/windows/macro-window/CLAUDE.md | `src/plugins/windows/macro-window/CLAUDE.md`（Day61 新規作成） |
| src/drivers/CLAUDE.md | `src/drivers/CLAUDE.md`（Day61 更新済み） |
| Transport Architecture spec | `docs/spec/transport-architecture.spec.md` |
| MacroKnob spec | `docs/spec/macro-knob.spec.md` |
| CC Mapping（SSoT） | `docs/spec/cc-mapping.md` |
| CC Map JSON（自動生成） | `settings/cc-map.json` |
| TransportEvent 型定義 | `src/types/index.ts` |
| TransportManager | `src/core/transportManager.ts` |
| TransportRegistry | `src/core/transportRegistry.ts` |
| AssignRegistry | `src/core/assignRegistry.ts`（旧 macroKnob.ts から改名） |
| Engine | `src/core/engine.ts` |
| ProjectManager | `src/core/projectManager.ts` |
| PresetStore | `src/core/presetStore.ts` |
| GeometrySimpleWindow | `src/plugins/windows/simple-window/GeometrySimpleWindow.tsx` |
| CameraSimpleWindow | `src/plugins/windows/simple-window/CameraSimpleWindow.tsx` |
| FxSimpleWindow | `src/plugins/windows/simple-window/FxSimpleWindow.tsx` |
| MacroWindow | `src/plugins/windows/macro-window/MacroWindow.tsx` |
| App.tsx | `src/ui/App.tsx` |
| PreferencesPanel | `src/ui/panels/preferences/PreferencesPanel.tsx` |

---

## 現在の状態

- **ブランチ**: `refactor/day53-design`
- **タグ**: `day60`（Day61 の CLAUDE.md 更新コミットは `0b84d15`・タグ未打）
- **最新コミット**: `0b84d15`（docs: update CLAUDE.md files to reflect Day61 refactoring）
- **テスト**: 114 tests グリーン・tsc エラーゼロ（Day60 ベースライン）

---

## Day61 で完了したこと

### CLAUDE.md 全面整合（Day58〜60 の実装に追従）

前チャットのフリーズにより2チャットに分けて実施。

| ファイル | 更新内容 |
|---|---|
| `src/core/CLAUDE.md` v7 | `assignRegistry.ts`（旧 macroKnob.ts）改名反映・TransportManager/Registry アーキテクチャ図更新 |
| `src/plugins/windows/CLAUDE.md` v3 | `simple-window/`・`macro-window/` ディレクトリ構成を現実に合わせて更新 |
| `src/plugins/windows/macro-window/CLAUDE.md` | 新規作成（MacroWindow の役割・MUST ルール・データフロー） |
| `src/ui/CLAUDE.md` v4 | SimpleWindow パスを `src/plugins/windows/simple-window/` に修正・MacroKnob Panel → MacroWindow |
| `src/ui/panels/CLAUDE.md` v3 | `macro-knob/` ディレクトリ・Panel 一覧から削除（格下げ反映） |
| `src/drivers/CLAUDE.md` | Input Driver と TransportManager の関係を追記（TransportEvent 経由フロー明文化） |
| ルート `CLAUDE.md` | `src/ui/CLAUDE.md` 参照説明を更新（MacroWindow 含む旨を追記） |

### アーカイブ
- `docs/archive/CLAUDE/2026-04-15_Day61_ui-CLAUDE.md`（旧 src/ui/CLAUDE.md v3）

---

## Day59・60 の主要実装（参照用）

### Day59（WindowPlugin 全面刷新）
- SimpleWindowPlugin / CameraWindowPlugin / FxWindowPlugin を自律化
- `transportRegistry.onChanged()` を直接購読・L1/L2/L3 タブ追加
- 旧 SimpleWindow（GeometrySimpleWindow・CameraSimpleWindow・FxSimpleWindow）全廃止→アーカイブ
- `registrationKey` 導入（`'layer-N:geometry'` / `'layer-N:camera'` / `'layer-N:fx'`）
- `TransportEvent` に `layerId?` 追加・source:'window' は該当レイヤーのみに書く

### Day60（Save/Load・Preset・FX レイヤー別対応）
- `projectManager.ts` / `presetStore.ts` 新設（App.tsx から分離）
- Save/Load でカメラ種別・FX 設定・全パラメータを正確に復元
- Preference FX L1/L2/L3 タブ追加・レイヤー別独立 FX 設定
- cc-mapping.md v0.4（5桁番号体系）全面刷新・83 mappings 再生成
- `applySceneState()` 新設・Open 後に全パラメータ復元

---

## 確定したアーキテクチャ（Day61 時点）

### Window 体系
| 分類 | 定義 | 場所 |
|---|---|---|
| Simple Window | Plugin のデフォルト最小 UI | `src/plugins/windows/simple-window/` |
| Macro Window | AssignRegistry へのノブ UI | `src/plugins/windows/macro-window/` |
| Mixer Simple Window | Mixer 専用 | `src/plugins/mixers/simple-mixer/` |

### Panel 体系（Day61 確定）
| Panel | 場所 |
|---|---|
| Preferences Panel | `src/ui/panels/preferences/` |
| ~~MacroKnob Panel~~ | → MacroWindow に格下げ済み |

### コアアーキテクチャ
```
MidiInputWrapper（drivers/input/）
  → TransportEvent { slot, value, source: 'midi' }
  → engine.handleMidiCC()
        ↓
  TransportManager → AssignRegistry 参照 → ParameterStore
        ↓
  engine.flushParameterStore() → TransportRegistry → Plugin.params
```

---

## 次回やること

### 優先度高
1. **ベースライン確認**
   ```bash
   cd /Users/shinbigan/geography && pnpm tsc --noEmit && pnpm test --run
   ```
2. **Day61 タグ打ち**
   ```bash
   git tag day61 && git push origin main --tags
   ```
3. **Sequencer spec 壁打ち**（Day60 から持ち越し）
   - TimelineLogger の仕様から決める
   - Sequencer のデータフォーマット設計

### 優先度中
4. `transportRegistry.value` の整理（挙動問題なし・設計冗長さのみ）
5. LICENSE ファイル追加（OSS 公開直前）

---

## 発生した問題と解決策

| 問題 | 解決策 |
|---|---|
| 前チャットがフリーズ（コミット前） | 新チャットで継続・CLAUDE.md 更新内容は保持されていた |
| HANDOVER.md が Day58 のまま | Day61 引き継ぎメモ作成後に HANDOVER.md を更新 |

---

## 環境メモ

- **ブランチ**: `refactor/day53-design`
- **ブラウザ確認**: `pnpm dev` → `open http://localhost:5173`（毎回再起動が必要）
- **NFC 正規化スクリプト**: `/Users/shinbigan/nfc_normalize.py`
- **git commit メッセージ**: 日本語長文は `.claude/dayN-commit.txt` に書いて `git commit -F` で実行
- **write_file ルール**: 既存ファイルには使わない・move_file → read → 差分提示 → 承認 → write_file

---

## 次回チャット用スタートプロンプト

```
Day62開始
```
