# GeoGraphy 引き継ぎメモ｜Day68（src/core/ 完全解体）｜2026-04-21

## 現在の状態

- **ブランチ**: `refactor/day53-design`
- **タグ**: `day68`（コミット: `d0c858f`）
- **テスト**: 127 tests グリーン・tsc エラーゼロ

## Day68 完了内容

### src/core/ 完全解体

shim（re-export）方式で5Stepに分けて17ファイルを application/ 以下へ移行。
各Step後に tsc + test グリーンを確認しながら進行。
最後に全 import を新パスに直接更新し、shim を削除。

**移行先まとめ:**
- `application/schema/` → config.ts, midiRegistry.ts
- `application/command/` → command.ts
- `application/orchestrator/` → engine.ts, layerManager.ts, fxStack.ts, programBus.ts, previewBus.ts
- `application/orchestrator/tempo/` → clock.ts
- `application/registry/` → registry.ts, transportManager.ts, transportRegistry.ts, assignRegistry.ts
- `application/registry/state/` → parameterStore.ts
- `application/catalog/` → ccMapService.ts
- `application/adapter/storage/` → presetStore.ts, projectManager.ts

src/core/ は CLAUDE.md のみ残存（実装ファイルゼロ）。

## 次回やること

1. **CLAUDE.md 更新**（src/core/ 解体を反映・新パス記載）
2. **Sequencer Window spec 検討**（Phase 16）
3. **film / frei-chen が FX_STACK_ORDER に未登録**（警告対応）
