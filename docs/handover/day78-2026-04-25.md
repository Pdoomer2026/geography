# GeoGraphy 引き継ぎメモ｜Day78｜2026-04-25

## プロジェクト概要
- アプリ名：GeoGraphy（No-Texture・Plugin駆動・マルチライブラリ映像制作プラットフォーム）
- 技術スタック：Vite / React 18 / TypeScript / Three.js r160+ / pnpm / Electron 41
- 開発スタイル：SDD × CDD・仕様書先行・handover-relay ワークフロー
- GitHub：https://github.com/Pdoomer2026/geography
- ブランチ：`refactor/day53-design`
- 直近タグ：`day78`

## 重要ファイルパス

| ファイル | パス |
|---|---|
| Handover memo | `docs/handover/day78-2026-04-25.md` |
| 要件定義書 | `docs/要件定義書_v2.1.md` |
| 実装計画書 | `docs/実装計画書_v3.2.md`（→ v4.1 未作成） |
| electron main | `electron/main.js` |
| electron preload | `electron/preload.js` |
| geoAPI 型定義 | `src/application/schema/geoAPI.d.ts` |
| App.tsx | `src/ui/App.tsx` |
| projectManager | `src/application/adapter/storage/projectManager.ts` |
| recentManager（新規） | `src/application/adapter/storage/recentManager.ts` |
| CLAUDE.md | `CLAUDE.md` |

## 今回のセッション（Day78）で完了したこと

### Electron 薄い鏡化 実装（メイン作業）
- `electron/main.js`: `menuOpen()` / `menuSaveAs()` / `menuOpenRecent()` を `sendToRenderer()` のみに変更
  - dialog / readFile / addRecent の呼び出しを全て削除
  - IPC ハンドラーを3つ新設：`add-recent` / `get-recent` / `clear-recent`
- `electron/preload.js`:
  - `onOpen` / `onSaveAs` を引数なしに変更（薄い鏡化）
  - `onOpenRecent(filePath)` を追加
  - `addRecent` / `getRecent` / `clearRecent` メソッドを追加
  - `removeMenuListeners` に `menu:open-recent` を追加
- `src/application/schema/geoAPI.d.ts`:
  - `onOpen` / `onSaveAs` の型を引数なしに変更
  - `onOpenRecent` 追加
  - `addRecent` / `getRecent` / `clearRecent` の型定義追加
- `src/application/adapter/storage/recentManager.ts`（新規作成）:
  - `geoAPI.addRecent` / `getRecent` / `clearRecent` の薄いラッパー
- `src/ui/App.tsx`:
  - `onOpen` を async に変更（renderer が `showOpenDialog → loadFile → addRecent` を実行）
  - `onSaveAs` を async に変更（renderer が `showSaveDialog → saveAs` を実行）
  - `onOpenRecent` を追加（renderer が `loadFile → openProject → addRecent` を実行）
- `src/application/adapter/storage/projectManager.ts`:
  - `save()` 内の誤った `onMenuEvents` コールバック登録を削除

### 要件定義書 v2.1 更新
- §3.3 / §3.4 の「⚠️ 要確認」マークを解除
- §3.3 を「実装の置き場所」と「オーケストレーション」で分離して記述
  - §3.3.1 メニューハンドラーは「薄い鏡」（Day78 確立）追記
  - §3.3.2 2つの薄い鏡（Day77 設計確定）追記
- §3.4 にレンダラー側の責務を10項目に拡張（Open/SaveAs/OpenRecent オーケストレーション等）

## 現在の状態（重要）

- **テスト**: 129 tests green
- **tsc**: エラー 0
- **コミット**: `a68cb5e`（docs 更新）/ `62c021f`（薄い鏡化実装）
- **タグ**: `day78` push 済み
- **ブランチ**: `refactor/day53-design`（最新 push 済み）
- **実装計画書**: v3.2 のまま（v4.1 未作成）

## 発生した問題と解決策

- 特になし（スムーズに実装完了）

## 次回やること（優先順位順）

1. **実装計画書 v4.1 新規作成**
   - v3.2 をアーカイブ → `docs/archive/実装計画書_v3.2.md`
   - Day78 の薄い鏡化・§3.3/3.4 確定を反映した v4.1 を新規作成
   - `docs/実装計画書_v4.1.md` として作成

2. **`film` / `frei-chen` の `FX_STACK_ORDER` 未登録バグ修正**
   - `src/core/fxStack.ts` の `FX_STACK_ORDER` に `film` / `frei-chen` を追加
   - `[FxStack] Unknown FX id` コンソール警告が出ている

3. **Phase 15: MacroKnob D&D アサイン UI**
   - `docs/spec/macro-knob.spec.md` §4 を読んでから実装
   - パラメーターロウに `[≡]` ドラッグハンドルを追加

4. **Phase 16: Sequencer Plugin**（MacroKnob D&D 完了後）

5. **Phase 17: Shader Plugin**（Sequencer 完了後）

## 設計メモ（Day78 確立）

### 2つの薄い鏡とは
```
【鏡 1】React UI メニュー（開発・Vercel デモ用）
  ボタンクリック → Application 層（projectManager）に直結

【鏡 2】Electron ネイティブメニュー（製品・.dmg 用）
  Menu click → sendToRenderer() のみ
  → App.tsx が geoAPI 経由で Application 層を呼ぶ

2つの鏡が同じ Application 層（projectManager）を参照する
```

### §3.3 / §3.4 の分離原則
- **§3.3（main.js）**：実装の置き場所（fs / dialog の実体）
- **§3.4（renderer）**：制御層・オーケストレーション（いつ・どの順で呼ぶか）

## 環境メモ

- `pnpm dev` 後は必ず `open http://localhost:5173` で新規ブラウザを開く（HMR 不可）
- 日本語ファイル書き込み後は `python3 /Users/shinbigan/nfc_normalize.py` を実行
- git commit の日本語複数行は `.claude/dayN-commit.txt` 経由で `git commit -F`
- `edit_file` は既存ファイルのみ / `write_file` は新規ファイルのみ
- 実装前に必ず計画を提示して承認を得る（すぐ実装しないこと）
