refactor: Day58 Step4 Registry コア層化・TransportManager 昇格

設計目標:
  「コア層が自律的に動く・UI は鏡」を実現

新規作成:
  src/core/transportRegistry.ts
    - Registry をコアシングルトンとして独立
    - slot → pluginId:paramId の対応表を保持
    - onChanged コールバックで UI に通知
    - syncValue() で flushParameterStore と連動

  src/core/transportManager.ts
    - MidiManager をプロトコル非依存に昇格
    - MIDI / OSC / 将来の入力全てに対応できる設計

engine.ts:
  - ccMapService 依存を initialize() + initTransportRegistry() に集約
  - flushParameterStore() を transportRegistry ベースに書き換え
  - resolveParamValue() / ccMapService への直接依存を完全削除
  - registerPluginToTransportRegistry() を公開 API として追加
  - midiManager → transportManager に切り替え

App.tsx:
  - ccMapService の import が完全に消えた
  - midiRegistry state が transportRegistry の「鏡」になった
  - registerParams / clearParams / syncValues の import が消えた

src/types/index.ts:
  - MidiManager interface のコメントを TransportManager として更新

テスト: 114 tests グリーン・tsc エラーゼロ
